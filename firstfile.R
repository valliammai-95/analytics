mtcars
head(mtcars) #first six rows
tail(mtacrs,2)
tail(mtcars,2) #last two rows
?mtcars #help
mean(mtcars$mpg) # $ signifies the row in which operations need to be performed
table(mtcars$gear)
table(mtcars$gear)
head(mtcars)
